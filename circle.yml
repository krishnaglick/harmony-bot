test:
    override:
        - node -v

deployment:
  push_to_server:
    branch: master
    commands:
      - chmod 777 pullCode.sh
      - nvm install 7.2.0
      - nvm use 7.2.0
      - ./pullCode.sh
      - npm i -g pm2 yarn

